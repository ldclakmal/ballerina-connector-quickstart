# Returns the max of given two numbers.
#
# + a - Number 1
# + b - Number 2
# + return - the max out of number 1 and number 2
public function max(int a, int b) returns int {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}
